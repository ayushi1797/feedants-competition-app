// Change this to your computer's LAN IP when testing on a physical phone.
export const API_BASE_URL = "http://10.0.2.2:5000/api";

export const DEMO_USER_ID = "66c000000000000000000001";

// After running `npm run seed` in the server folder, replace the value below
// with the Competition ObjectId printed by the seed command.
export const DEMO_COMPETITION_ID = "REPLACE_WITH_COMPETITION_ID";
