Submission notes
The project is designed around the requirements in the Feedants technical assignment.
The mobile client does not hardcode competition availability. It reads the current state from the API.
The backend is responsible for business rules, while MongoDB stores competition and participation data.
For a real deployment, the demo user ID would be replaced by authenticated user information and the API would be hosted over HTTPS.
